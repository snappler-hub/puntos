<div class="row">
  <fieldset class="form-group col-md-5">
    <%= f.label :vademecum %>
    <%= f.collection_select :vademecum_id, Vademecum.all, :id, :name, {}, {class: 'form-control', id: 'js-vademecumField'} %>
  </fieldset>
</div>

<div class="row">
  <div class="col-md-7"> <%= label_tag :products, 'Producto' %> </div>
  <div class="col-md-3"> <%= label_tag :products, 'Cantidad' %> </div>
</div>

<div id="js-productPfpcs" class="product_pfpcs  form-group">
  <%= f.fields_for :product_pfpcs do |p| %>
    <%= render 'product_pfpc_fields', f: p %>
  <% end %>
  <div class="links">
    <%= link_to_add_association 'Nuevo Producto', f, :product_pfpcs, class: 'btn btn-default' %>
  </div>
</div>

<%= content_for(:form_help) do %>
  <div class="box box-widget">
    <!-- HEADER -->
    <div class="box-header with-border">
      <a href="#">Notas</a>
      <div class="box-tools">
        <button type="button" class="btn btn-box-tool" data-widget="remove"><i class="fa fa-times"></i></button>
      </div>
    </div>
    
    <!-- BODY -->
    <div class="box-body">
      <p>
        Los productos serán filtrados por el Vademecum seleccionado.
      </p>

      <p>
        Los productos previamente añadidos a otro servicio no están disponibles para la selección.
      </p>
    </div>
  </div>
<% end %>