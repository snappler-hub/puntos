<!DOCTYPE html>
<html lang="es">
  <head><%= render 'head' %></head>
  <body class="skin-blue fixed <%= controller_name %> <%= action_name %>">
    <div class="wrapper">

      <%= render 'navbar' %>

      <aside class="main-sidebar">
        <section class="sidebar">
          <%= render 'side' %>
        </section>
      </aside>

      <article class="content-wrapper">
        <%= yield %>
        <section id="snackbar-container">
          <%= flash_messages if flash %>
        </section>
      </article>

      <footer class="main-footer">
        <div class="pull-right hidden-xs">
          <b>Version</b>
          <%= Plenario::Application::VERSION %>
        </div>
        <strong>Manes</strong>
      </footer>

    </div>

  </body>
</html>