<h4> Productos </h4>

<div class="product_pfpcs">
  <%= f.fields_for :product_pfpcs do |p| %>
    <%= render 'product_pfpc_fields', f: p %>
  <% end %>
  <div class="links">
    <%= link_to_add_association 'Nuevo Producto', f, :product_pfpcs %>
  </div>
</div>