<tr>
  <td><%= product.code %></td>
  <td><%= product.name %></td>
  <td><%= product.points %></td>
  <td>
    <%= link_to 'Editar', edit_product_path(product), class: 'btn btn-xs btn-success' %>
    <%= link_to 'Eliminar', product, method: :delete, data: {confirm: '¿Está seguro?'}, class: 'btn btn-xs btn-danger' %>
  </td>
</tr>